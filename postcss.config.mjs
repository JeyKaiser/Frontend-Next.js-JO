// Este archivo es un módulo de JavaScript que exporta la configuración de PostCSS.
// que se utilizará en el proyecto.
// Importa el plugin de Tailwind CSS para PostCSS
// y lo configura para que se use en el proyecto.

const config = {
  plugins: ["@tailwindcss/postcss"],
};

export default config;
